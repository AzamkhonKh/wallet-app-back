package com.example.wallet.core.domain;

public enum TransactionType {
    INCOME,
    EXPENSE,
    TRANSFER // Note: Handling TRANSFER logic (linking transactions/spaces) is more complex and omitted for brevity here.
}