# ===================================================================
# Operating System Files
# ===================================================================
.DS_Store
Thumbs.db
desktop.ini

# ===================================================================
# Build Output (Maven / Gradle)
# ===================================================================
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
build/
.gradle/
*.jar
*.war
*.ear
*.zip
*.tar.gz
*.rar

# ===================================================================
# IDE Configuration Files
# ===================================================================
# IntelliJ IDEA
.idea/
*.iml
*.ipr
*.iws
out/
shelf/ # IntelliJ shelf

# Eclipse
.classpath
.project
.settings/
bin/
*.tmp
*.bak
*.swp
*~.nib

# NetBeans
/nbproject/private/
/build/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/

# VS Code
.vscode/
*.code-workspace

# ===================================================================
# Logs and Temporary Files
# ===================================================================
*.log
logs/
*.log.*
hs_err_pid*
*.dump

# ===================================================================
# Runtime Data
# ===================================================================
pids/
*.pid
*.seed
*.pid.lock

# ===================================================================
# Sensitive Information / Local Environment
# ===================================================================
.env          # Environment variables file (IMPORTANT!)
*.env
.env.*
secrets/
*.pem
*.key
credentials.*
application-local.* # Common pattern for local-only config

# ===================================================================
# Specific Tool Files
# ===================================================================
# Maven Wrapper
mvnw.cmd # Windows Maven wrapper script (sometimes committed, depends on team preference)
# If you generated wrapper with `mvn wrapper:wrapper`, .mvn/wrapper/ contents are usually committed.

# Gradle Wrapper
gradlew.bat # Windows Gradle wrapper script
# If you generated wrapper, .gradle/ and gradle/wrapper contents are usually committed.

# Terraform
.terraform/
*.tfstate
*.tfstate.*

# Terragrunt
.terragrunt-cache*/

# Packer
packer_cache/
*.box

# Docker (Usually not needed if build context is managed, but sometimes...)
docker-compose.override.yml # Often used for local-only overrides

# ===================================================================
# Misc
# ===================================================================
# Ignore file itself if desired (though usually committed)
# .gitignore

# Optional: Node build artifacts if using frontend build tools
# node_modules/
# npm-debug.log*
# yarn-debug.log*
# yarn-error.log*
# /dist/
# /public/

# Optional: Python artifacts
# __pycache__/
# *.py[cod]
# *$py.class